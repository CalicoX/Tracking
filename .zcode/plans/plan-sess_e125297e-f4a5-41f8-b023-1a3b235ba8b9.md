# 只改文案（09-08 文档 + 标注图），Features 保留手风琴壳改 4 项

原则：文本替换为主，不加动画、不上图片。改完 `npx vitest run`（只修锁旧文案的断言）+ 起 dev server 自查 1440/390 + 更新 memory + 本地 commit（不 push）。

## 逐文件改动

**1. Hero.jsx**
- h1 → `Build Post-Purchase Customers Want to Come Back To`（按文档原样；疑似缺 Experience 一词，先照抄）
- lead 替换 → `Turn every delivery touchpoint into a branded experience that keeps customers informed, engaged, and connected — from shipment to repeat purchase and loyalty.`
- CTA casing → `Start Free Trial` / `Book A Demo`；cta-note 不动

**2. TrustBand.jsx**：删副标行 `Post-purchase infrastructure for DTC, cross-border, and Shopify merchants.`（文档「删除副标题」红框在这张图上），只留 Trusted by 100,000+ 行

**3. ImpactBand.jsx**：3.3 → `3.5`（Loyalty lift）。不加 CTA 行（TODO 7 留后续）

**4. ProductDock.jsx**：`17 Order Tracking → Order Tracking`、`17 Returns → 17RETURNS`。Topbar 不动

**5. FeaturesSection.jsx（按你的更正）**
- **只删 section-head**（"Everything merchants need…" 标题 + lead + 里面的 CTA 行）；手风琴壳、sticky、面板机制全保留
- 手风琴 3 项 → 4 项，文案按文档卖点 1-4：每项 h3 = 标签（BRANDED TRACKING PAGE / BRANDED EMAIL NOTIFICATION / SPLIT-ORDER MANAGEMENT / CONVERSION & LOYALTY）+ 一句描述 + 勾选 bullets（描述加一个 `feature-desc` 小容器）
- 右侧面板按新顺序重排：tab0 品牌页 mock（AURA 表单那组）、tab1 邮件通知 mock（现 last-mile 邮件+追踪卡组）、tab2 split mock、tab3 Conversion & Loyalty 无动画图（下一批）→ 放一张极简静态占位卡（16% Repeat purchases + 文档 bullets 作 chips，用现有 fx-glass 词汇）
- tab 接线是 landing-inline.js 按 `data-feature` 索引泛化处理的，4 项无需改 JS
- `structure.test.js` L132-136：三条旧标题断言换四条新标签；`not data-feature="3"` 反转为「允许 3、禁止 4」；mock 断言（Last-mile Carrier / Package #1 / You may also like / Visit store）都还在，不动

**6. AiLab.jsx**
- 新增 eyebrow 标签 `AI-POWERED POST-PURCHASE EXPERIENCE`（AI 二字包 `ai-word-ai` span——新标题没有 AI 字，缩放特效的查询源挪到这里，否则 ai-letter-zoom 查不到元素特效会死）
- intro 标题 → `Tracking Is Getting Smarter. So is the Customer Journey.`
- intro lead（现为内部指令文案）→ `AI brings smarter prediction, personalization, and engagement to the post-purchase journey — from delivery estimates and intelligent tracking experiences to opportunities that drive repeat purchase.`
- agent 卡 lead → `AI will automatically generate a template that matches your brand based on this page.`
- 5 步标题 → Reading brand site / Understanding brand story / Extracting visual style / Designing custom page / Fine-tuning & applying（细节行保留）；`ai-lab.js` 若有依赖步骤字符串的逻辑同步
- CTA1 → `Try AI-generator`，CTA2 casing → `Book A Demo`

**7. ExploreMore.jsx**
- h2 → `Find the Right Solution for Your Business`；lead → `Go beyond tracking with solutions for post-purchase experience, returns automation, and global shipment visibility.`
- Returns 卡：h3 `17RETURNS`、加标题行 `Turn Returns Into Revenue and Growth`、描述换文档句、链接 `Explore 17RETURNS`
- API 卡：h3 `Tracking Developer API`、加标题行 `Power Your Systems With Global Tracking Visibility`、描述换 3,500+ carriers 句、链接 `Explore Tracking API`（文案不变只查 casing）
- 卡内 mock 不动

**8. BottomCta.jsx**
- h2 → `Growing LTV along the way`；段落（现为内部指令文案）→ `Turn every post-purchase touchpoint into an opportunity to drive repeat purchases and customer value.`
- 删 `View pricing` 按钮；加一行数据条（静态小样式）：`Top Global Carriers Coverage` ／ 4,000+ Carriers Worldwide ／ 9+30 Standardized Shipment Status ／ 99.9% Tracking Accuracy ／ 95%+ Carrier Recognition Success Rate ／ 99.9% SLA；CTA casing 对齐文档

**9. Footer.jsx**：WHO WE ARE 段落 → `17TRACK has been helping merchants track orders, provide real-time updates, and boost repeat sales since 2010.`

**10. BrandsSay.jsx**：两颗 CTA casing → `Start Free Trial` / `Book A Demo`（其余不动）

## 不做（这轮）
布局重排、ImpactBand/各模块补双 CTA（TODO 7）、动画提速（TODO 5）、板块顺序调整、功能/AI 动画图、returns/API 两仓同步。

## 验证与收尾
- `npx vitest run` 全绿；dev server（5175）自查 1440/390 无破版、AI 缩放特效仍工作
- 更新 `.cursor/memory.md`（新文案基准、AI 缩放源挪 eyebrow、手风琴 4 项、占位卡待换动画图）
- 本地 commit，不 push